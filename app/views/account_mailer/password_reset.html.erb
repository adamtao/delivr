<p>Hi. We received a request to reset your password for <%= ENV['SITE_NAME'] %>.</p>

<p>Please click on the link below to continue:<p>

<p><%= link_to 'Reset Password', reset_password_url(@identity.password_reset_digest) %></p>

<p>(If that doesn't work, try copying and pasting the link into your browser)</p>

<p><%= reset_password_url(@identity.password_reset_digest) %></p>

<p>If you didn't initiate the password reset request, you can safely ignore this message.</p>
